package Trilemma;

import java.util.List;
import java.util.ArrayList;
import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.SqlUtils;
import de.greenrobot.dao.internal.DaoConfig;

import Trilemma.ARMOR_INVENTORY;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table ARMOR__INVENTORY.
*/
public class ARMOR_INVENTORYDao extends AbstractDao<ARMOR_INVENTORY, Long> {

    public static final String TABLENAME = "ARMOR__INVENTORY";

    /**
     * Properties of entity ARMOR_INVENTORY.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property Number = new Property(1, Integer.class, "number", false, "NUMBER");
        public final static Property Enhanced_point = new Property(2, Integer.class, "enhanced_point", false, "ENHANCED_POINT");
        public final static Property Update_datetime = new Property(3, java.util.Date.class, "update_datetime", false, "UPDATE_DATETIME");
        public final static Property Armor_id = new Property(4, Long.class, "armor_id", false, "ARMOR_ID");
    };

    private DaoSession daoSession;


    public ARMOR_INVENTORYDao(DaoConfig config) {
        super(config);
    }
    
    public ARMOR_INVENTORYDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
        this.daoSession = daoSession;
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "'ARMOR__INVENTORY' (" + //
                "'_id' INTEGER PRIMARY KEY ," + // 0: id
                "'NUMBER' INTEGER," + // 1: number
                "'ENHANCED_POINT' INTEGER," + // 2: enhanced_point
                "'UPDATE_DATETIME' INTEGER," + // 3: update_datetime
                "'ARMOR_ID' INTEGER);"); // 4: armor_id
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "'ARMOR__INVENTORY'";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, ARMOR_INVENTORY entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        Integer number = entity.getNumber();
        if (number != null) {
            stmt.bindLong(2, number);
        }
 
        Integer enhanced_point = entity.getEnhanced_point();
        if (enhanced_point != null) {
            stmt.bindLong(3, enhanced_point);
        }
 
        java.util.Date update_datetime = entity.getUpdate_datetime();
        if (update_datetime != null) {
            stmt.bindLong(4, update_datetime.getTime());
        }
 
        Long armor_id = entity.getArmor_id();
        if (armor_id != null) {
            stmt.bindLong(5, armor_id);
        }
    }

    @Override
    protected void attachEntity(ARMOR_INVENTORY entity) {
        super.attachEntity(entity);
        entity.__setDaoSession(daoSession);
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public ARMOR_INVENTORY readEntity(Cursor cursor, int offset) {
        ARMOR_INVENTORY entity = new ARMOR_INVENTORY( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getInt(offset + 1), // number
            cursor.isNull(offset + 2) ? null : cursor.getInt(offset + 2), // enhanced_point
            cursor.isNull(offset + 3) ? null : new java.util.Date(cursor.getLong(offset + 3)), // update_datetime
            cursor.isNull(offset + 4) ? null : cursor.getLong(offset + 4) // armor_id
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, ARMOR_INVENTORY entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setNumber(cursor.isNull(offset + 1) ? null : cursor.getInt(offset + 1));
        entity.setEnhanced_point(cursor.isNull(offset + 2) ? null : cursor.getInt(offset + 2));
        entity.setUpdate_datetime(cursor.isNull(offset + 3) ? null : new java.util.Date(cursor.getLong(offset + 3)));
        entity.setArmor_id(cursor.isNull(offset + 4) ? null : cursor.getLong(offset + 4));
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(ARMOR_INVENTORY entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(ARMOR_INVENTORY entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
    private String selectDeep;

    protected String getSelectDeep() {
        if (selectDeep == null) {
            StringBuilder builder = new StringBuilder("SELECT ");
            SqlUtils.appendColumns(builder, "T", getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T0", daoSession.getARMORDao().getAllColumns());
            builder.append(" FROM ARMOR__INVENTORY T");
            builder.append(" LEFT JOIN ARMOR T0 ON T.'ARMOR_ID'=T0.'_id'");
            builder.append(' ');
            selectDeep = builder.toString();
        }
        return selectDeep;
    }
    
    protected ARMOR_INVENTORY loadCurrentDeep(Cursor cursor, boolean lock) {
        ARMOR_INVENTORY entity = loadCurrent(cursor, 0, lock);
        int offset = getAllColumns().length;

        ARMOR aRMOR = loadCurrentOther(daoSession.getARMORDao(), cursor, offset);
        entity.setARMOR(aRMOR);

        return entity;    
    }

    public ARMOR_INVENTORY loadDeep(Long key) {
        assertSinglePk();
        if (key == null) {
            return null;
        }

        StringBuilder builder = new StringBuilder(getSelectDeep());
        builder.append("WHERE ");
        SqlUtils.appendColumnsEqValue(builder, "T", getPkColumns());
        String sql = builder.toString();
        
        String[] keyArray = new String[] { key.toString() };
        Cursor cursor = db.rawQuery(sql, keyArray);
        
        try {
            boolean available = cursor.moveToFirst();
            if (!available) {
                return null;
            } else if (!cursor.isLast()) {
                throw new IllegalStateException("Expected unique result, but count was " + cursor.getCount());
            }
            return loadCurrentDeep(cursor, true);
        } finally {
            cursor.close();
        }
    }
    
    /** Reads all available rows from the given cursor and returns a list of new ImageTO objects. */
    public List<ARMOR_INVENTORY> loadAllDeepFromCursor(Cursor cursor) {
        int count = cursor.getCount();
        List<ARMOR_INVENTORY> list = new ArrayList<ARMOR_INVENTORY>(count);
        
        if (cursor.moveToFirst()) {
            if (identityScope != null) {
                identityScope.lock();
                identityScope.reserveRoom(count);
            }
            try {
                do {
                    list.add(loadCurrentDeep(cursor, false));
                } while (cursor.moveToNext());
            } finally {
                if (identityScope != null) {
                    identityScope.unlock();
                }
            }
        }
        return list;
    }
    
    protected List<ARMOR_INVENTORY> loadDeepAllAndCloseCursor(Cursor cursor) {
        try {
            return loadAllDeepFromCursor(cursor);
        } finally {
            cursor.close();
        }
    }
    

    /** A raw-style query where you can pass any WHERE clause and arguments. */
    public List<ARMOR_INVENTORY> queryDeep(String where, String... selectionArg) {
        Cursor cursor = db.rawQuery(getSelectDeep() + where, selectionArg);
        return loadDeepAllAndCloseCursor(cursor);
    }
 
}
